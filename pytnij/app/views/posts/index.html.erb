<h1>Zapytaj</h1>

<br />

<!--
<%= render :partial => @posts %>
-->

<table class="table table-striped">
  <tr>
    <th><h4>Tytuł</h4></th>
    <th><h4>Autor</h4></th>
    <th><h4>Liczba opini</h4></th>
  </tr>
<% @posts.each do |post| %>
  <tr>
  <td><%=link_to post.title , post %></td>
  <td><a href="mailto:#"><%= post.email %></a></td>
  <td><%= post.comments.count %></td>
  </tr>
<% end %>
</table>
<br />
<% if user_signed_in? %>
<hr />

<% end %>

